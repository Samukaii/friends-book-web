import { ButtonAutoLoadingDirective } from './button-auto-loading.directive';

describe('ButtonAutoLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonAutoLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
